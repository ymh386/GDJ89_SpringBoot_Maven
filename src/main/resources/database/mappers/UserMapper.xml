<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="com.winter.app.user.UserDAO">
  
  	<insert id="join" parameterType="UserVO">
  		INSERT INTO USER_TBL
  		(USERNAME, PASSWORD,NAME, PHONE, EMAIL, BIRTH, FILE_NAME, ORI_NAME)
  		VALUES
  		(#{username}, #{password},#{name} #{phone}, #{email}, #{birth}, #{fileName}, #{oriName})
  	</insert>
  	
  	<select id="detail" parameterType="UserVO" resultType="UserVO">
  		SELECT * FROM USER_TBL
  		WHERE USERNAME=#{username}
  	</select>
  	
  </mapper>